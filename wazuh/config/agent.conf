<agent_config>
  <localfile>
    <location>Microsoft-Windows-Sysmon/Operational</location>
    <log_format>eventchannel</log_format>
  </localfile>

  <!-- Disabled: agent-side SCA (CIS Windows benchmark) generates one alert per failed check
       (300+ on a typical desktop), same flood risk as the manager-side SCA. Out of scope for
       this project. See docs/decisions.md. -->
  <sca>
    <enabled>no</enabled>
  </sca>
</agent_config>
